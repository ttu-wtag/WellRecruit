<nav class="navbar navbar-expand-lg bg-body-tertiary">
  <div class="container">
    <b><%= link_to "WellRecruit", root_path, class: "navbar-brand"%></b>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      <ul class="navbar-nav me-auto mb-2 mb-lg-0">
        <li class="nav-item">
          <a class="nav-link" href="#">Home</a>
        </li>
        <li class="nav-item">
          <a class="nav-link" href="#">Jobs</a>
        </li>
      </ul>
      <form class="d-flex" role="search">
        <% if user_signed_in? %>
          <%= link_to "Edit Profile", edit_user_registration_path, method: :get, class: "btn btn-outline-dark" %> &nbsp;
          <%= button_to "Log out", destroy_user_session_path, method: :get, class: "btn btn-outline-dark" %>
        <% else %>
          <%= link_to "Sign in", new_user_session_path, method: :get, class: "btn btn-outline-dark" %>&nbsp;
          <%= link_to "Sign up", new_user_registration_path, method: :get, class: "btn btn-outline-success" %>
        <% end %>
      </form>
    </div>
  </div>
</nav>
